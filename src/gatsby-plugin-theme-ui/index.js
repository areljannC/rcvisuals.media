import { base } from '@theme-ui/presets'

const theme = {
  ...base,
  colors: {
    ...base.colors,
    text: '#040404',
    background: '#FFFFFF',
    primary: '#FF5D1A',
    secondary: '#1B66A5',
    muted: '#FFA47E',
  },
}

export default theme
