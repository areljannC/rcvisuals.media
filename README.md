# Gatsby Default Starter Lite Plus

🧹 A Gatsby.js starter based on the default starter without the boilerplate.

## File Structure

This starter follows the `page-container` file structure pattern.

```bash
src
├── containers
├── pages
├── shared
│   ├── components
│   └── utils
```

---